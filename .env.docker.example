POSTGRES_DB=amielar
POSTGRES_USER=amielar
POSTGRES_PASSWORD=change_me_local_only

# URL-encode reserved characters in the password before placing it in this URL.
DATABASE_URL=postgresql://amielar:change_me_local_only@db:5432/amielar

ADMIN_USERNAME=local_admin
ADMIN_PASSWORD_HASH=replace_with_local_scrypt_hash
ADMIN_SESSION_SECRET=replace_with_at_least_32_random_bytes
APP_ORIGIN=http://localhost:8080

HTTP_PORT=8080
